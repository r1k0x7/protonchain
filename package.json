{
  "name": "proton-chain-solidity",
  "version": "0.1.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:devnet": "hardhat run scripts/deploy.js --network proton_devnet",
    "node": "hardhat node",
    "coverage": "hardhat coverage"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0",
    "hardhat": "^2.19.0"
  }
}
